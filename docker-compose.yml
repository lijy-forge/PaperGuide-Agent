name: paperguide

x-python-environment: &python-environment
  PYTHONUNBUFFERED: "1"
  PYTHONDONTWRITEBYTECODE: "1"
  PAPERGUIDE_MODE: ${PAPERGUIDE_MODE:-production}
  PAPERGUIDE_LLM_PROVIDER: ${PAPERGUIDE_LLM_PROVIDER:-openai}
  PAPERGUIDE_MODEL_NAME: ${PAPERGUIDE_MODEL_NAME:-gpt-5.4}
  PAPERGUIDE_EXPORT_DIRECTORY: /app/data/artifacts
  PAPERGUIDE_MAX_PAPERS: ${PAPERGUIDE_MAX_PAPERS:-10}
  PAPERGUIDE_LOG_LEVEL: ${PAPERGUIDE_LOG_LEVEL:-INFO}

services:
  host:
    build:
      context: .
      dockerfile: docker/host/Dockerfile
    image: paperguide-host:local
    init: true
    restart: unless-stopped
    environment:
      <<: *python-environment
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_BASE_URL: ${OPENAI_BASE_URL:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      TAVILY_API_KEY: ${TAVILY_API_KEY:-}
      SEMANTIC_SCHOLAR_API_KEY: ${SEMANTIC_SCHOLAR_API_KEY:-}
    volumes:
      - paperguide-data:/app/data
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "from paperguide.runtime import RuntimeSettings; from paperguide.runtime.host import SQLiteHostBroker; settings=RuntimeSettings.from_env(); raise SystemExit(0 if SQLiteHostBroker(settings.task_database_path()).host_available() else 1)"
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 30s
    cpus: 2.0
    mem_limit: 3g
    stop_grace_period: 45s

  api:
    build:
      context: .
      dockerfile: docker/api/Dockerfile
    image: paperguide-api:local
    init: true
    restart: unless-stopped
    command:
      - uvicorn
      - paperguide.api.app:create_default_api_app
      - --factory
      - --host
      - 0.0.0.0
      - --port
      - "8000"
    environment:
      <<: *python-environment
    volumes:
      - paperguide-data:/app/data
    ports:
      - "127.0.0.1:${PAPERGUIDE_API_PORT:-8000}:8000"
    depends_on:
      host:
        condition: service_healthy
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health/live', timeout=3).read()"
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 15s
    cpus: 1.0
    mem_limit: 1g

  dashboard:
    build:
      context: .
      dockerfile: docker/dashboard/Dockerfile
      args:
        VITE_PAPERGUIDE_API_BASE_URL: ${VITE_PAPERGUIDE_API_BASE_URL:-http://localhost}
    image: paperguide-dashboard:local
    restart: unless-stopped
    ports:
      - "127.0.0.1:${PAPERGUIDE_DASHBOARD_PORT:-80}:80"
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--output-document=-", "http://127.0.0.1/healthz"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
    cpus: 0.5
    mem_limit: 256m

volumes:
  paperguide-data:
    name: paperguide-data
